export default class Bin {
  constructor(contentType, photo, capacity, maxCapacity) {
    this.contentType = contentType;
    this.photo = photo;
    this.trashPhoto = trashPhoto;
    this.capacity = capacity;
    this.maxCapacity = maxCapacity;

  }
}
